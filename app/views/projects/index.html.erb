<h1>Projects</h1>

<% @projects.each do |project| %>
  <p></p><%= project.title %></p>
  <%= link_to 'Edit', edit_project_path(project) %>
  <p></p><%= project.subtitle %></p>
  <p></p><%= project.body %></p>
  <%= image_tag project.thumb_image if !project.thumb_image.nil? %>
<% end %>